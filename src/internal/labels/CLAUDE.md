# labels — domain module

Rules: ../../../docs/architecture/archetypes/domain-module.md

Module-specific notes:
- Empty stub: only an empty `adapters/` directory exists, no Go files. The module is not wired into `server.go`.
- Decision pending — either flesh out as a domain module or remove until needed. See `docs/architecture/refactor-backlog.md`.

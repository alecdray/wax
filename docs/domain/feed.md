# Feed — domain

Background sync of external sources (Spotify) into the user's album relationships. Owns feed sync state, scheduling, backoff, and inbox playlist handles.

*Scope to be filled in a follow-up `docs/spec/` chunk.*